name: Spell-check
on: [push, pull_request]
jobs:
  spellcheck:
    uses: open-contracting/.github/.github/workflows/spellcheck.yml@main
    permissions:
      contents: read
    with:
      ignore: fo,nam,sme,zar
      skip: docson,locale,pdf,currency.csv
